지금부터 이 프로젝트의 컨텍스트를 세팅할 거야.
아래 규칙에 따라 `CLAUDE.md`와 메모리 시스템을 구성하고, 이후 대화 내내 이 규칙을 유지해줘.

---

### 1. CLAUDE.md 세팅

프로젝트 루트에 `CLAUDE.md`를 생성해줘.
지금 당장 모든 내용을 채울 수 없는 항목은 `<!-- TODO -->` 주석으로 표시하고, 대화가 진행되면서 파악되는 대로 알아서 채워줘.

아래 템플릿을 기반으로 작성해:

~~~markdown
# CLAUDE.md

## 클로드 자율 운영 규칙

이 파일과 메모리 시스템은 클로드가 자율적으로 유지 관리한다.
별도 요청 없이도 아래 규칙을 항상 실행할 것.

### CLAUDE.md 업데이트
- 새로운 기술 스택, 디렉토리 구조, 의사결정 등 어떠한 형태로든 파악된 추가적인 정보가 이후 작업에서 참고할 여지가 있다고 판단될 때마다 적극적으로 해당 섹션을 업데이트한다.

### 메모리 구조
모든 지식 파일(.md)은 `.claude/memory/` 하나로 통합한다. 기획서, 설계 문서, 클로드가 생성한 메모리 파일 모두 이 안에 둔다.
MEMORY.md 인덱스만 두 곳에 동기화한다.
- 인덱스 원본: `~/.claude/projects/<project-path>/memory/MEMORY.md`
- 인덱스 사본: `.claude/memory/MEMORY.md`

MEMORY.md를 수정할 때마다 두 경로를 동시에 업데이트한다. 동기화 누락 없음.
모든 파일 경로는 두 MEMORY.md 모두 프로젝트 절대 경로로 작성한다. URL 인코딩(%20 등) 없이 공백은 그대로 사용한다.

### MEMORY.md 인덱스 형식
인덱스는 카테고리별로 구성한다. 카테고리와 파일은 파악된 내용에 따라 자유롭게 생성하고, 필요하면 언제든 재구성한다.
`.claude/memory/` 내부의 디렉토리 구조도 파일이 늘어나면 자유롭게 재구성한다.

인덱스 형식:

    # Memory Index

    ## <카테고리명>
    - [파일 제목](/절대경로/.claude/memory/파일명.md) — 한 줄 요약

---

## 프로젝트 정보
<!-- TODO -->
~~~

CLAUDE.md는 프로젝트가 진행되면서 새로운 사실이 파악될 때마다 자유롭게 업데이트해줘.

---

### 2. 초기 파일 생성

아래 순서로 초기 세팅을 진행해줘.

1. `.claude/` 하위에 `memory/` 외의 폴더가 있으면 그 안의 `.md` 파일들을 `.claude/memory/` 안으로 이동시켜줘. 디렉토리 구조는 적절히 판단해서 정리해.
2. 위 CLAUDE.md를 프로젝트 루트에 생성해줘.
3. CLAUDE.md 안의 "자율 운영 규칙"에 따라 메모리 초기 파일들을 만들어줘. 현재 파악된 정보로 채울 수 있는 항목은 채우고, 나머지는 `<!-- TODO -->`로 남겨둬.
